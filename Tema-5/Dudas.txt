Buenas Mati, te escribo para comentarte de que no puedo resolver la actividad individual 3 del bloque tematico 5. 
La seleccionar las palabras de un texto que comienzan con n, s, a y d.
Hice algo, pero realmente no se muy bien que estoy haciendo.


Ademas arranque con la actividad grupal del mismo bloque tematico, hice mas que nada el esquema de la estructura, siento que el problema 
esta en el objeto stock. Haciendo unas modificaciones logre sacarar el precio actualizado de la leche por ejemplo, pero sacar el precio
de cada producto individualmente no me parece la forma mas acertada. 

---------

Hola Fede!!

Sobre lo primero, siguiendo tu enfoque, no estás mal orientado. Algunos detallecitos:

texto.split(" ") = words;

La línea es al revés:

words = text.split(" "); <-- el resultado de aplicar el método split devuelve un array de fragmentos de string.

Luego el for, debería ser:

for ( word of words )
{
   //Acá estarías recorriendo cada fragmento del string.
   if ( word[0] == 'n' || word[0] == 's' || ... etc.. )
   {
      result.push(word);
   }
};

return result;

Donde result sería un array de resultados.

------

El problema de la leche, vuelvo a revisar y te escribo nuevamente!